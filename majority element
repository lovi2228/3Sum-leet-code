class Solution {
public:
    int majorityElement(vector<int>& nums) {

        map<int ,int >map;
        for(auto i:nums){
            map[i]++;
        }
        for(auto i:map){
            if(i.second>(nums.size()/2)) return i.first;
        }
        return 0;
    }
};
